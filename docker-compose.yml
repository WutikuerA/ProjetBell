version: '3.4'

services:
    msnet6:
        image: msnet6
        build:
            context: ./ProjetBellAPI
            dockerfile: ./Dockerfile
        ports:
         - 5010:80
    
    angular:
        image: angular
        build:
            context: ./projet-bell-webapp/projet-bell-angular
            dockerfile: ./Dockerfile
        ports:
         - 4200:80
        environment:
            "BASE_URL": "http://localhost:5010"